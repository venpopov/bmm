% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{SDTdist}
\alias{SDTdist}
\alias{dSDT}
\alias{pSDT}
\alias{qSDT}
\alias{rSDT}
\title{Models of Signal Detection Theory (SDT)}
\usage{
dSDT(
  x,
  size,
  dprime = 1,
  crit = dprime/2,
  stimulus = 1,
  dist_noise = "normal",
  log = FALSE
)

pSDT(
  q,
  size,
  dprime = 1,
  crit = dprime/2,
  stimulus = 1,
  dist_noise = "normal",
  lower.tail = TRUE,
  log.p = FALSE
)

qSDT(
  p,
  size,
  dprime = 1,
  crit = dprime/2,
  stimulus = 1,
  dist_noise = "normal",
  lower.tail = TRUE,
  log.p = FALSE
)

rSDT(n, size, dprime = 1, crit = dprime/2, stimulus = 1, dist_noise = "normal")
}
\arguments{
\item{x}{A vector of observed responses.}

\item{size}{The number of trials used to collect the responses.}

\item{dprime}{A vector of numerical values for the signal strength.}

\item{crit}{A vector of numerical values for the decision criterion.}

\item{stimulus}{A character vector ("signal"/"noise", "old"/"new") or
integer vector (1/0) coding which stimuli were shown alongside the observed responses,
or for which to generate data for.  If no information is provided for \code{rSDT} then we will
assume the first half of the to be generated trials as 1 and the second half as 0.}

\item{dist_noise}{A string specifying which noise distribution is assumed for the
Signal-Detection Model. Available options are "normal", "gumbel", "cauchy", and "logistic".
The default is "normal".}

\item{log}{logical. if \code{TRUE} , values are returned on the log scale.}

\item{q}{A vector of quantiles.}

\item{lower.tail}{logical. if \code{TRUE} (default), probabilities are P[X < x],
otherwise P[X > x].}

\item{log.p}{logical. if \code{TRUE} , values are returned on the log scale.}

\item{p}{A vector of probabilities.}

\item{n}{Number of observations to generate data for.}
}
\description{
Density, distribution, and random generation function for SDT models for binary (yes/no)
responses without confidence ratings. All functions use the parameters \code{dprime} for the
signal strength and \code{crit} for the criterium for saying "yes" or "old" to a stimulus.
}
\examples{
# example code


}
\keyword{distribution}
